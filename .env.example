# Redis Configuration
REDIS_URL=redis://localhost:6379

# Cloudflare R2 Storage (for file attachments)
R2_ACCOUNT_ID=your_account_id_here
R2_ACCESS_KEY_ID=your_access_key_here
R2_SECRET_ACCESS_KEY=your_secret_key_here
R2_BUCKET=kiq-attachments
R2_PUBLIC_BASE_URL=https://pub-xxxxxxxxxx.r2.dev

# Audit Signing (JWS signatures for tamper-proof audit log)
AUDIT_SIGNING_SECRET=your_secret_key_here_32_bytes_minimum

# Packet Workflow Policy
# Dual-approval threshold: events >= this amount require 2 distinct approver signatures
# Set to 0 to disable dual-approval (default: 0)
KIQ_DUAL_APPROVAL_THRESHOLD=100000

# Authentication Configuration
# Development mode: set to "dev" to use headers for testing
# Production mode: remove or set to "prod" to enforce JWT verification
KIQ_AUTH_MODE=dev

# Development Auth Defaults (only used when KIQ_AUTH_MODE=dev)
KIQ_DEFAULT_USER_ID=dev-user-001
KIQ_DEFAULT_USER_EMAIL=admin@kiq.local
KIQ_DEFAULT_USER_ROLE=admin

# Production Auth (JWT verification)
# Replace with your auth provider's configuration:
# - Auth0: AUTH0_DOMAIN, AUTH0_CLIENT_ID, AUTH0_CLIENT_SECRET
# - Clerk: CLERK_SECRET_KEY
# - Supabase: SUPABASE_JWT_SECRET
# - Custom JWT: JWT_SECRET